package com.aether.aether_backend.domain;

/**
 * Origin of a connection: discovered automatically by the embedding pipeline,
 * or created manually by the user (drag &amp; drop).
 */
public enum ConnectionOrigin {
    AUTO,
    MANUAL
}
